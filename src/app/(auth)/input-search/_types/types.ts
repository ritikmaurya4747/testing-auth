export type Contact = {
    _id: string;
    name: string;
    email: string;
};
export type TableContactProps = {
  searchTerm: string;
};
export type ContactData = {
  _id: string;
  name: string;
  email: string;
};
